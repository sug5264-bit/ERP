# ============================================================
# Supabase PostgreSQL
# ============================================================
# Transaction 모드 (Connection Pooler) - 애플리케이션 쿼리용
# Supabase Dashboard > Settings > Database > Connection string > URI (Transaction mode)
# 주의: 비밀번호에 특수문자(@, !, # 등)가 있으면 반드시 URL 인코딩 필요
#   @ → %40, ! → %21, # → %23
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Session 모드 (Direct) - Prisma 마이그레이션용
# Supabase Dashboard > Settings > Database > Connection string > URI (Session mode)
DIRECT_URL="postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres"

# ============================================================
# NextAuth
# ============================================================
# AUTH_SECRET 생성: openssl rand -base64 32
AUTH_SECRET="generate-with-openssl-rand-base64-32"
# Vercel 배포 후 실제 도메인으로 변경 필요
AUTH_URL="https://your-app.vercel.app"
NEXTAUTH_URL="https://your-app.vercel.app"

# ============================================================
# 운영 설정 (대기업 프로덕션용)
# ============================================================
# 로그 레벨: debug | info | warn | error
LOG_LEVEL="info"

# 서비스 식별자 (분산 환경에서 로그 구분용)
SERVICE_NAME="erp-system"

# 느린 쿼리 임계값 (ms) - 이 값을 초과하면 로그에 경고 기록
SLOW_QUERY_THRESHOLD="200"

# Node.js 환경 (production | development)
NODE_ENV="production"
